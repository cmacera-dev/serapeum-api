## What

<!-- What changes, and what it fixes. Link the issue: "Closes #123". -->

## Why

<!-- The reason, not the diff. What was wrong, or what was missing? -->

## How it was verified

<!--
What you actually ran, and what it printed. "Should work" is not verification.
For a bug fix, the most useful line is the reproduction *before* the fix.
-->

- [ ] `npm run test:run` (tests + coverage thresholds)
- [ ] `npm run typecheck` / `npm run lint` / `npm run format:check`
- [ ] Verified the behaviour, not just that it compiles

## Anything reviewers should push back on

<!--
Trade-offs, things you were unsure about, or scope you deliberately left out.
Say so here rather than hoping nobody notices.
-->
